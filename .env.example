# OceanBus L0 API base URL (defaults to production)
OCEANBUS_URL=https://ai.ihaola.com.cn/api/l0

# L1 service public OB address — NOT a secret, like a public server address on the OB network.
# Without this, the skill falls back to local calendar and 20-story fallback corpus.
LUCKY_LOBSTER_SVC_OPENID=
